# AGENTS.md

## Agent skills（智能体技能）

### Issue tracker（问题跟踪器）

Issue 与 spec 以本地 markdown 文件形式存放在 `.scratch/<feature>/` 目录下。详见 `docs/agents/issue-tracker.md`。

### Triage labels（分拣标签）

五个标准角色：`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix`。详见 `docs/agents/triage-labels.md`。

### Domain docs（领域文档）

单 context：根目录 `CONTEXT.md` + `docs/adr/`。详见 `docs/agents/domain.md`。
